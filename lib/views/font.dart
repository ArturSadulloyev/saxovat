import 'package:flutter/material.dart';

font(
    {double size = 16.0,
    FontWeight weight = FontWeight.w400,
    Color color = Colors.black}) {
  return TextStyle(
    fontSize: size,
    fontWeight: weight,
    color: color,
  );
}
